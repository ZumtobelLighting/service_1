apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "my-product-helm-chart.fullname" . }}
  labels:
    {{- include "my-product-helm-chart.labels" . | nindent 4 }}
spec:
  rules:
    - host: {{ .Values.ingress.host }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ include "my-product-helm-chart.fullname" . }}
                port:
                  number: {{ .Values.service.port }}